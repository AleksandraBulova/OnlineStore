import { FC } from "react";
import { SectionProducts } from "../../components/SectionProducts";

export const MainPage: FC = () => {
  return (
    <main>
      <h2>Main page</h2>
      <SectionProducts />
    </main>
  );
};
